# 2026-09-21 — SSH 历史受限 key 提示（本地候选）

`ssh-key-only` README 和替换警告仍要求重新安装已退役的 Egern/LAN Worker 自建上报 key。
本次只把当前提示改为历史用途、官方上报不依赖、先核对用途与备份，不建议重新启用。
key 分类、备份、add/replace 行为、authorized_keys/sshd/nftables 实现均未改变。

此项仅为可逆文案修正，不新增镜像文案的测试或调整 CI 范围。验证采用差异复核、文档链接
检查和目标脚本 `bash -n`，没有运行 SSH 维护器或其它恢复测试。
这些检查通过不代表实际 key 或服务器已核验。未删除/轮换/重新安装 key，也未 push/部署。

实施可单独应用此候选；文案回滚不会修改密钥，但不应重新推荐退役功能。

## 2026-09-22 本地应用

用户点名授权后，三文件候选在既有 `main` 精确应用并形成独立本地提交。
应用前 before/after SHA256 均匹配；只有本日期说明超出原候选字节。
目标 shell 语法、文档链接及 diff 检查通过。原 key 兼容/备份行为未改；
没有执行 SSH 维护器、真实主机或恢复操作，未 push、发布或部署。
